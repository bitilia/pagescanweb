{
  "name": "pagescan",
  "version": "1.0.0",
  "private": true,
  "description": "Generate printable sheets with corner registration marks, then scan them back into a PDF. The app itself has no dependencies and no build step; these are for the test suite only.",
  "scripts": {
    "test": "node test/roundtrip.js && node test/browser.js",
    "test:geometry": "node test/roundtrip.js",
    "test:browser": "node test/browser.js",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
